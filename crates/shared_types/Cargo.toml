[package]
name = "shared_types"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Shared cross-service DTOs and contracts for the Azums platform."

[dependencies]
execution_core = { path = "../execution_core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
