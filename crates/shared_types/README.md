# shared_types

Shared cross-service DTOs for Azums.

Current module:

- `shared_types::status_api`
  - Request/receipt/history/callback/job/intake-audit/replay response types
  - Callback destination request/response types

This crate is intended to avoid duplicated API type definitions across services.
