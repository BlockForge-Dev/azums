apiVersion: v1
kind: Service
metadata:
  name: ingress-api
  namespace: azums
  labels:
    app: ingress-api
spec:
  selector:
    app: ingress-api
  ports:
    - name: http
      port: 8081
      targetPort: 8081
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ingress-api
  namespace: azums
  labels:
    app: ingress-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ingress-api
  template:
    metadata:
      labels:
        app: ingress-api
    spec:
      containers:
        - name: ingress-api
          image: ghcr.io/blockforge-dev/azums/ingress_api:main
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8081
              name: http
          envFrom:
            - configMapRef:
                name: azums-platform-config
            - secretRef:
                name: azums-platform-secrets
          readinessProbe:
            httpGet:
              path: /health
              port: 8081
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8081
            initialDelaySeconds: 20
            periodSeconds: 15
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 1Gi
